import { Layout } from "@/components/layouts/Layout";
import Style from "../styles/Home.module.css";

export default function Populares() {
  return (
    <div>
      <Layout>
        <h1 className={Style.texto}>populares</h1>
      </Layout>
    </div>
  );
}
